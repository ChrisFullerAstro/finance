version: '2'
services:
  web:
    build: .
    command: python app/app.py
    ports:
      - "5000:5000"
    volumes:
      - .:/finance
    depends_on:
      - db
    restart: always
  db:
    image: mongo
    # volumes:
    # - ./db/:/data/db
  # mongoexpress:
  #   image: mongo-express
  #   ports:
  #     - "8081:8081"
  #   depends_on:
  #     - db
